---
import Layout from '../layouts/Layout.astro'
import MainContent from '../components/MainContent.astro'
import PageHeader from '../components/text/PageHeader.astro'
---

<Layout title="Robby Webb | Contact">
  <MainContent>
    <PageHeader text="Contact" />
    <form method="POST" class="form" name="contactForm" action="/success" data-netlify="true">
      <div class="form-field">
        <input id="name" type="text" name="name" placeholder=" " required />
        <label for="name">Name: <span>*</span></label>
      </div>
      <div class="form-field">
        <input id="email" type="email" name="email" placeholder=" " required />
        <label for="email">Email: <span>*</span></label>
      </div>
      <div class="form-field">
        <textarea id="message" name="message" cols="30" rows="5" placeholder=" " required
        ></textarea>
        <label for="message">Message: <span>*</span></label>
      </div>
      <button class="site-cta button" type="submit">Send Message</button>
    </form>
  </MainContent>
</Layout>

<style>
  .form {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-paragraph);
    margin: 0 auto var(--spacing-section);
    max-width: 800px;
  }

  .form-field {
    position: relative;
    display: flex;
    flex-direction: column;
    margin-bottom: 2.5rem;
  }

  .form-field textarea {
    resize: vertical;
  }

  .form-field input,
  .form-field textarea {
    padding: 0.5rem;
    color: var(--color-white);
    background-color: var(--color-black);
    font-size: var(--size-sm);
    font-weight: 200;
    border-bottom: 2px solid var(--color-white);
    border-top: none;
    border-left: none;
    border-right: none;
    border-radius: 0;
    outline: none;
    transition: all var(--transition-normal);
  }

  .form-field input:focus,
  .form-field textarea:focus {
    border-bottom: 2px solid var(--color-pink);
  }

  .form-field input:not(:placeholder-shown):valid,
  .form-field textarea:not(:placeholder-shown):valid {
    border-color: var(--color-white);
  }

  .form-field input:not(:placeholder-shown):not(:valid),
  .form-field textarea:not(:placeholder-shown):not(:valid) {
    border-color: var(--color-pink);
  }

  .form-field label {
    position: absolute;
    top: 48%;
    left: 0.4rem;
    transform: translateY(-50%);
    font-size: var(--size-sm);
    color: var(--color-grey);
    transition: all var(--transition-normal);
  }

  .form-field textarea + label {
    top: 1.5rem;
    color: var(--color-grey);
  }

  .form-field input:focus + label,
  .form-field textarea:focus + label {
    top: -1rem;
    left: 0;
    font-size: var(--size-xs);
    color: var(--color-pink);
  }

  .form-field input:valid + label,
  .form-field textarea:valid + label {
    color: var(--color-white);
  }

  .form-field input:not(:placeholder-shown) + label,
  .form-field textarea:not(:placeholder-shown) + label {
    top: -1rem;
    left: 0;
    font-size: var(--size-xs);
  }

  .form-field input:not(:placeholder-shown):not(:valid) + label,
  .form-field textarea:not(:placeholder-shown):not(:valid) + label {
    color: var(--color-pink);
  }

  .form-field label span {
    color: var(--color-pink);
  }
</style>
